<!DOCTYPE html>
<html lang="en-US">
  <head>
    <meta charset="utf-8">
    <title><%= htmlWebpackPlugin.options.title %></title>
    <style>
        body {
            background: linear-gradient(135deg, #0f172a 0%, #1e3a8a 100%);
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        #spinner,
        #spinner:after {
            border-radius: 50%;
            width: 50vmin;
            height: 50vmin;
        }
        #spinner {
            font-size: 10px;
            position: fixed;
            top: 50%;
            left: 50%;
            text-indent: -9999em;
            pointer-events: none;
            border-top: 1.1em solid rgba(59, 130, 246, 0.3);
            border-right: 1.1em solid rgba(59, 130, 246, 0.3);
            border-bottom: 1.1em solid rgba(59, 130, 246, 0.3);
            border-left: 1.1em solid #3b82f6;
            transform: translateZ(100);
            animation: load8 1.1s infinite linear;
            box-shadow: 0 0 20px rgba(59, 130, 246, 0.5);
        }
        @-webkit-keyframes load8 {
            0% {
                transform: translate(-50%, -50%) rotate(0deg);
            }
            100% {
                transform: translate(-50%, -50%)  rotate(360deg);
            }
        }
        @keyframes load8 {
            0% {
                transform: translate(-50%, -50%) rotate(0deg);
            }
            100% {
                transform: translate(-50%, -50%) rotate(360deg);
            }
        }
    </style>
  </head>
  <body>
    <div id="spinner">Loading...</div>
  </body>
</html>